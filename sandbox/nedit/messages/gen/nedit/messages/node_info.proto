syntax = "proto3";

option optimize_for = LITE_RUNTIME;

package nil.nedit.proto;

import "type.proto";

message NodeInfo {
    message ControlInfo
    {
        message SliderControlInfo
        {
            float min = 1;
            float max = 2;
            float value = 3;
        }

        message SpinboxControlInfo
        {
            int32 min = 1;
            int32 max = 2;
            int32 value = 3;
        }

        message ToggleControlInfo
        {
            bool value = 1;
        }

        message TextControlInfo
        {
            string value = 1;
        }

        oneof control {
            ToggleControlInfo toggle = 2;
            SpinboxControlInfo spinbox = 3;
            SliderControlInfo slider = 4;
            TextControlInfo text = 5;
        }
    }

    repeated uint64 inputs = 1;
    repeated uint64 outputs = 2;
    repeated ControlInfo controls = 3;
    string label = 4;
}
